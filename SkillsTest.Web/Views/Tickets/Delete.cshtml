@using SkillsTest.Web.Infrastructure
@model SkillsTest.Web.ViewModels.Tickets.DeleteViewModel
@{
    ViewBag.Title = "Delete";
}
@section TitleSection{
    <div id="page-title">
        <h1 class="page-header text-overflow">Tickets</h1>
    </div>
}
@section BreadcrumbSection{
    <ol class="breadcrumb">
        <li><a href="~/">Home</a></li>
        <li><a href="~/Tickets">Tickets</a></li>
        <li class="active">Delete Ticket</li>
    </ol>
}
@section StyleSection{}
@section ScriptSection{}
<div class="row">
    <div class="col-sm-12">
        <div class="panel">
            <div class="panel-heading">
                <h3 class="panel-title">Ticket Delete Form</h3>
            </div>
            @using (Html.BeginForm("Delete", "Tickets", new { id = Model.Ticket.Id }, FormMethod.Post, new { @class = "form-horizontal form-padding", role = "form" }))
            {
                <div class="panel-body">
                    @Html.AntiForgeryToken()
                    @Html.AbcideeValidationSummary()
                    <div class="alert alert-danger">
                        <strong>Alert!</strong> Are you sure you want to delete this Ticket?
                    </div>
                    <div class="form-group">
                        <label class="col-md-3 control-label" for="Ticket_Title">Title</label>
                        <div class="col-md-9">
                            @Html.EditorFor(model => model.Ticket.Title, new { htmlAttributes = new { @class = "form-control", placeholder = "Enter title", disabled = "disabled" } })
                            @Html.AbcideeValidation("Ticket.Title")
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="col-md-3 control-label" for="Ticket_Description">Description</label>
                        <div class="col-md-9">
                            @Html.TextAreaFor(model => model.Ticket.Description, new { @class = "form-control", placeholder = "Enter description", rows = "6", disabled = "disabled" })
                            @Html.AbcideeValidation("Ticket.Description")
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="col-md-3 control-label" for="Ticket_Priority">Priority</label>
                        <div class="col-md-9">
                            @Html.EnumDropDownListFor(model => model.Ticket.Priority, htmlAttributes: new { @class = "form-control", disabled = "disabled" })
                            @Html.AbcideeValidation("Ticket.Priority")
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="col-md-3 control-label" for="Ticket_Status">Status</label>
                        <div class="col-md-9">
                            @Html.EnumDropDownListFor(model => model.Ticket.Status, htmlAttributes: new { @class = "form-control", disabled = "disabled" })
                            @Html.AbcideeValidation("Ticket.Status")
                        </div>
                    </div>
                </div>
                <div class="panel-footer text-right">
                    <a class="btn btn-warning" href="~/tickets/edit?id=@Model.Ticket.Id">Cancel</a>
                    <button class="btn btn-danger" type="submit">Confirm Delete</button>
                </div>
            }
        </div>
    </div>
</div>
